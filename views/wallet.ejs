<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>EduSphere | Wallet</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Manrope:wght@300..800&family=Space+Grotesk:wght@400..700&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/styles/style.css" />
  </head>
  <body class="wallet-page">
    <%- include("partials/navbar") %>
    <main class="section wallet-section">
      <div class="container">
        <header class="wallet-hero">
          <div>
            <p class="eyebrow text-uppercase text-muted mb-2">Digital wallet</p>
            <h1 class="display-4 fw-bold mb-2">Top up and pay with confidence</h1>
            <p class="text-muted lead">
              Load funds instantly via PayPal, card, or bank transfer, and keep everything processed
              inside the wallet.
            </p>
          </div>
          <div class="wallet-pill">
            <p class="text-muted small mb-1">Available balance</p>
            <strong class="display-5">$<%= Number(walletBalance || 0).toFixed(2) %></strong>
            <p class="text-muted small mb-0">Auto-will use funds first, then external methods.</p>
          </div>
        </header>

        <section class="wallet-grid">
          <div class="wallet-card stack-card">
            <h5 class="mb-3">Top-up options</h5>
            <form method="POST" action="/wallet/topup" class="wallet-topup-form">
              <div class="form-floating mb-3">
                <input
                  class="form-control"
                  id="amount-wallet"
                  name="amount"
                  type="number"
                  min="1"
                  step="0.01"
                  placeholder="Amount"
                  required
                />
                <label for="amount-wallet">Amount (USD)</label>
              </div>
              <div class="form-floating mb-3">
                <select
                  class="form-select"
                  id="payment_method_wallet"
                  name="payment_method"
                >
                  <option value="wallet">Digital wallet (instant)</option>
                  <option value="paypal">PayPal</option>
                  <option value="bank_transfer">Bank transfer</option>
                  <option value="card">Debit/Credit card</option>
                </select>
                <label for="payment_method_wallet">Payment method</label>
              </div>
              <button class="btn btn-gradient w-100 fs-6" type="submit">Top up now</button>
            </form>
            <p class="text-muted small mt-3">
              Spending uses wallet funds first, then fails over to PayPal/card automatically.
            </p>
          </div>

          <div class="wallet-card stack-card">
            <h5 class="mb-3">Payment guidance</h5>
            <ul class="text-muted small guidance-list">
              <li>Wallet charges are instant, while bank transfers may take up to 24 hrs.</li>
              <li>PayPal/card toggles help keep you covered if your balance runs low.</li>
              <li>Transactions sync automatically with your dashboard history.</li>
            </ul>
            <div class="mt-auto text-end">
              <a href="/courses" class="link-primary small">Browse courses to spend your wallet</a>
            </div>
          </div>
        </section>

        <% if (status?.topup_success) { %>
          <div class="alert alert-success">Top-up complete.</div>
        <% } %>
        <% if (status?.topup_error) { %>
          <div class="alert alert-danger">Enter a valid amount.</div>
        <% } %>

        <section class="wallet-card transactions card stack-card">
          <div class="card-body">
            <div class="d-flex justify-content-between align-items-center mb-3">
              <h5 class="card-title mb-0">Recent transactions</h5>
              <span class="text-muted small"><%= transactions.length %> entries</span>
            </div>
            <div class="table-responsive">
              <table class="table table-sm table-borderless align-middle mb-0 wallet-table">
                <thead>
                  <tr>
                    <th>ID</th>
                    <th>Type</th>
                    <th>Amount</th>
                    <th>Status</th>
                    <th>Date</th>
                  </tr>
                </thead>
                <tbody>
                  <% if (!transactions || transactions.length === 0) { %>
                    <tr>
                      <td colspan="5" class="text-muted small">No transactions yet.</td>
                    </tr>
                  <% } else { %>
                    <% transactions.forEach(function(tx) { %>
                      <tr>
                        <td><%= tx.id %></td>
                        <td class="text-capitalize"><%= tx.type.replace(/_/g, " ") %></td>
                        <td>$<%= Number(tx.amount).toFixed(2) %></td>
                        <td class="text-capitalize"><%= tx.status %></td>
                        <td><%= tx.created_at ? new Date(tx.created_at).toLocaleString() : "" %></td>
                      </tr>
                    <% }); %>
                  <% } %>
                </tbody>
              </table>
            </div>
          </div>
        </section>
      </div>
    </main>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
