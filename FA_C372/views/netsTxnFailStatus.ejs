<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>EduSphere | NETS Failed</title>
    <link rel="stylesheet" href="/styles/style.css" />
  </head>
  <body class="course-page">
    <main class="section">
      <div class="container">
        <div class="dashboard-panel text-center">
          <h1>NETS Payment Failed</h1>
          <p class="text-muted"><%= message || "Transaction failed." %></p>
          <a class="btn btn-outline" href="<%= backUrl || '/cart' %>"><%= backLabel || "Back" %></a>
        </div>
      </div>
    </main>
      <%- include("partials/footer") %>
  </body>
</html>
