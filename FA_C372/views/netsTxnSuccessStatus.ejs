<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>EduSphere | NETS Success</title>
    <link rel="stylesheet" href="/styles/style.css" />
  </head>
  <body class="course-page">
    <main class="section">
      <div class="container">
        <div class="dashboard-panel text-center">
          <h1>NETS Payment Successful</h1>
          <p class="text-muted"><%= message || "Payment successful." %></p>
          <% if (txnRetrievalRef) { %>
            <p class="text-muted small">Reference: <%= txnRetrievalRef %></p>
          <% } %>
          <a class="btn btn-primary" href="<%= backUrl || '/cart?payment_ready=nets' %>"><%= backLabel || "Back" %></a>
        </div>
      </div>
    </main>
  </body>
</html>
