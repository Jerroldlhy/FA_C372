<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= course.course_name %> | Learning</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/styles/style.css" />
  </head>
  <body class="course-page">
    <%- include("partials/navbar") %>
    <main class="section">
      <div class="container">
        <div class="course-hero mb-4">
          <div>
            <a class="btn btn-link px-0" href="/learning">&larr; Back to My Learning</a>
            <h1 class="display-6 fw-bold mb-1"><%= course.course_name %></h1>
            <p class="text-muted mb-0">Instructor: <%= course.instructor_name || "TBA" %></p>
          </div>
          <div class="course-hero-card">
            <p class="text-muted small mb-1">Progress</p>
            <strong class="display-6"><%= progress %>%</strong>
            <p class="text-muted small mb-0">Track your completion by lesson.</p>
          </div>
        </div>

        <% if (status?.progress_updated) { %>
          <div class="alert alert-success">Progress updated.</div>
        <% } %>

        <section class="dashboard-panel mb-4">
          <h5>Protected course content</h5>
          <p class="text-muted mb-0">
            This page is accessible only if you are enrolled. Content preview:
            <%= course.description || "Course content will be provided by your lecturer." %>
          </p>
        </section>

        <section class="dashboard-panel">
          <h5 class="mb-3">Lessons</h5>
          <div class="table-responsive">
            <table class="table align-middle mb-0">
              <thead>
                <tr>
                  <th>Lesson</th>
                  <th>Status</th>
                  <th></th>
                </tr>
              </thead>
              <tbody>
                <% lessons.forEach(function(lesson) { %>
                  <tr>
                    <td><%= lesson.title %></td>
                    <td>
                      <% if (lesson.completed) { %>
                        <span class="badge bg-success">Completed</span>
                      <% } else { %>
                        <span class="badge bg-secondary">Pending</span>
                      <% } %>
                    </td>
                    <td class="text-end">
                      <form method="POST" action="/learning/<%= course.id %>/lessons/<%= lesson.lessonNo %>/complete">
                        <button
                          class="btn btn-sm btn-outline-primary"
                          type="submit"
                          <%= lesson.completed ? "disabled" : "" %>
                        >
                          Mark completed
                        </button>
                      </form>
                    </td>
                  </tr>
                <% }); %>
              </tbody>
            </table>
          </div>
        </section>
      </div>
    </main>
  </body>
</html>
